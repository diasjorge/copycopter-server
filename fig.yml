web:
  build: .
  ports:
    - "3000:3000"
  links:
    - postgres
  volumes:
    - ".:/app"

postgres:
  image: 'postgres'
  environment:
    POSTGRES_USER: 'copycopter'
    POSTGRES_PASSWORD: 'copycopter-dev'